# Product

## 一句话范围

**改一个条件，看同一场冲击会不会更糟。** EventShock Lab 比较同一事件的基准模拟与单一条件干预模拟，帮助用户检查模型内部的风险传播差异；它不预测现实市场。

## Users

主要用户是资管机构、银行和交易所的市场事件风险分析人员与研究团队。他们需要在时间有限、证据来源复杂且结论必须可复核的工作环境中，比较重大事件在单一受控干预下如何传播，并向同事解释结果的证据强度与局限。

次要用户是教授或学习行为金融、市场结构和模型治理的教师与学生。界面应让首次接触系统的学习者完成核心流程，同时保留专业研究人员所需的参数、版本、审计和复现信息。

产品不把个人投资者作为投资决策用户，也不根据用户的持仓、风险偏好或财务状况提供个性化结论。是否适合机构生产风险流程仍需真实目标用户访谈、独立领域审查和外部验证；这些证据当前均不得被描述为已完成。

## Product Purpose

EventShock Lab 让用户检查“只改变一个条件，同一场模拟冲击会怎样变化”。系统用来源可追溯的 Event Pack、配对随机种子和确定性市场机制执行事件驱动的反事实压力测试；核心任务是帮助用户提出一个研究问题、只改变一个干预变量、比较结果分布、追踪机制链路，并导出可复现实验工件。

成功意味着用户能够区分情景分析与预测，理解指标的不确定性和模型边界，并在不依赖项目团队口头指导的情况下完成一轮可审计实验。

## Use Boundaries

EventShock Lab 不提供，也不得通过界面、AI 解释或导出暗示以下结论：

- 应该买入、卖出或持有何种真实资产；
- 应该在何时交易，或真实资产的目标价格；
- 模拟路径会在现实世界出现，或干预与结果之间已建立现实因果关系；
- 某项投资能够获得收益、避免损失或达到任何胜率；
- 当前历史案例已经完成独立校准、样本外检验或外部有效性验证。

系统输出只能用于比较已声明模型假设下的情景差异。任何现实决策都需要产品之外的独立数据、专业判断、合规流程与责任主体。

## Interaction Modes & Authority

- **AI 引导模式**面向首次使用者，把事件目标、来源收集、来源审核、主张审核、事件包元数据、冻结、单一干预与运行前检查拆成有界阶段。AI 只能提出可编辑草案；“应用建议”只复制结构化内容到当前草稿，不得自动搜索、创建、冻结、提交或运行任何研究对象。
- **专家模式**面向希望直接控制参数的研究人员，保留 Event Pack Factory、事件包审核、情景、认知、指标与停止规则等手动界面。
- 注册后的模式推荐只来自用户自报的经验、协助偏好和目标，不通过测试推断能力，也不锁定选择。用户可以选择与推荐不同的模式，并在后续切换。
- 无论使用哪种模式，来源批准、候选主张编辑/批准、事件包冻结、单一干预确认和实验提交都必须由人明确执行。AI 建议不是事实、授权或最终决策。

## Data & AI Boundaries

- Event Pack Factory 的粘贴与 Reader 原文保存在按账号隔离的内部 payload 表，普通快照、日志、审计详情和导出不包含正文；owner 只能通过 no-store 专用接口按需展开。正文修订必须新建 revision、重扫安全并重置人工审核。构建从最近一次实质修改起保留 7 天并在 Factory 请求时清理；拒绝来源或删除构建会删除对应原文并尝试截断 WAL，但不追溯删除已经物化的 Event Pack。
- Search、Reader 与 Event Pack 物化使用 `clientRequestId + payload hash` 持久幂等：成功可恢复、同键异载荷冲突、计费结果未知时不自动重放。
- 搜索摘要只用于发现，不能作为主张证据。联网流程必须经过“搜索候选记录批准 → Reader 获取完整正文 → Reader 证据单独批准 → 物化待审核 Event Pack → 人工主张审核与冻结”。
- 普通用户的 API Key 只存在于当前登录会话的服务端内存。部署指定的唯一管理员可主动选择服务器持久保存；数据库只存认证加密密文，浏览器、日志、审计和导出不保存或返回明文，主机 root、Docker 管理员与应用进程仍属于信任边界。高级模型参数仅允许固定白名单及供应商能力子集；禁止自定义 Base URL、请求头、工具、系统提示词和任意请求体。
- 系统提示词作为源码的一部分可被审阅，运行时安全不依赖提示词保密。系统通过不可信数据分隔、确定性预扫描、严格结构与证据/动作/时间白名单、泄漏检测、有限修复和失败关闭降低提示词注入风险；无法承诺完全免疫，因此保留人工审核和确定性执行边界。

## Brand Personality

严谨、克制、证据优先。文案应具体说明数据从哪里来、模型做了什么、哪些结论不能推出；语气保持专业而易懂，不夸大 AI 能力，也不以技术术语制造权威感。

## Anti-references

- 不应像消费级聊天玩具，避免气泡尾巴、夸张拟人化、装饰性动画和随意语气。
- 不应像交易信号或价格预测终端，避免暗示买卖建议、确定性预测或现实因果结论。
- 不应像原始调试控制台，普通用户无需直接面对内部证据 ID、工具名或未解释的结构字段。
- 不应为追求视觉冲击而削弱信息密度、可访问性或结果可复核性。

## Design Principles

1. 先展示结论边界，再展示解释；任何重要结果都能回到对应证据区段。
2. 渐进披露复杂度；新用户先看到可读解释，技术标识和审计细节保留在明确的折叠层级中。
3. 使用一致、熟悉的研究工具交互；视觉差异表达角色和状态，不以装饰替代信息结构。
4. 保持配对实验、版本、来源和局限可见；界面不得把模型生成内容伪装成已验证事实。
5. 中英文提供等价功能和责任边界，不能只翻译标题而遗漏操作说明或错误状态。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为默认目标。所有功能必须支持键盘操作、清晰焦点、非颜色单一表达、语义化标题和可读的屏幕阅读器名称；正文对比度至少 4.5:1。所有非必要动画尊重 `prefers-reduced-motion`，移动端和高缩放场景不能出现内容溢出或不可达操作。
