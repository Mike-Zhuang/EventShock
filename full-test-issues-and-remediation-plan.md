# EventShock Lab 全流程问题清单与针对性修复方案

> - 编写日期：2026-07-29（America/Los_Angeles）
> - 文档性质：本轮修复开始前的完整问题基线、根因审计与实施计划
> - 覆盖范围：产品定位、AI 引导、Event Pack Factory、证据审核、情景构建、运行前检查、规则与 LLM 仿真、结果展示、结果解释助手、机制链路、治理、历史导出、账号与隐私、部署运维
> - 重要说明：本文只记录问题和解决方案，**本次不修改业务代码**。

---

## 0. 文档目的与使用方式

本项目已经具备一条真实可运行的端到端链路，但“功能存在”不等于“用户能理解、能顺利完成、能正确解释”。2026-07-29 的完整人工测试同时暴露了三类问题：

1. **会让正常链路直接中断的可靠性问题**，例如 AI 引导一次模型失败后永久锁住当前工作流版本；
2. **会让用户误解结果或做出错误结论的问题**，例如解释助手把跨零区间写成“显著扩大”；
3. **功能正确但表达、状态和交互不清楚的问题**，例如已登录用户仍看到“匿名浏览器会话历史”。

本文将每个问题写成可执行工程项，并尽量包含：

- 复现现象；
- 当前状态；
- 影响；
- 已核对的代码或数据证据；
- 根因判断；
- 有针对性的解决方案；
- 验收标准；
- 与其他问题的依赖关系。

本文与根目录的 `CHECKPOINT1_FEEDBACK_IMPROVEMENT_PLAN.md` 分工如下：

- `CHECKPOINT1_FEEDBACK_IMPROVEMENT_PLAN.md` 主要吸收课堂和 Checkpoint 1 外部反馈；
- 本文主要记录真实端到端测试、源码审计和生产运行中暴露的问题；
- 两份文档相互补充，不应把其中任意一份当作另一份的替代品。

---

## 1. 状态与优先级定义

### 1.1 严重级别

| 级别 | 定义 |
| --- | --- |
| P0 | 阻断主链路、可能导致安全事故、重复计费、错误研究结论或对外严重误导；修复前不应宣称对应功能可用于正式研究 |
| P1 | 正常用户高概率遇到，显著影响完成率、可解释性、数据一致性或可信度 |
| P2 | 不阻断核心流程，但明显降低效率、理解程度、可访问性或专业完成度 |
| P3 | 低风险一致性、文案、视觉和维护性问题 |

### 1.2 当前状态

| 状态 | 含义 |
| --- | --- |
| 当前已复现 | 在 2026-07-29 的部署版本或当前源码中仍可确认 |
| 源码已确认 | 未重新执行完整 UI 复现，但源码逻辑足以确认问题存在 |
| 历史已复现，需回归 | 之前出现过，当前代码已有改善，需要纳入回归测试 |
| 设计缺口 | 不一定表现为报错，但当前设计不足以达到产品目的 |
| 当前运行正常 | 曾经出过故障，本次只读检查显示当前正常，仍需长期回归 |

---

## 2. 本轮测试基线与关键证据

### 2.1 正常规则链路

本轮从 AI 引导开始，围绕“阿拉斯加航空 1282 航班门塞脱落事件”完成了以下链路：

1. 创建研究目标；
2. 选择联网搜索和原文来源；
3. 构建并人工审核 Event Pack；
4. 冻结 Event Pack；
5. 创建单变量情景；
6. 设置 `marketMakerCapacity: 1 → 0.65`；
7. 使用 10 对匹配随机种子、56 个 Agent、120 个仿真步；
8. 完成 `RULE_ONLY` 实验；
9. 查看结果、机制链路和治理信息；
10. 导出并重放可复现 ZIP。

规则实验 ID 为 `exp-384e1cd016604701`。导出包包含 23 个文件，ZIP CRC 检查正常；在 CPython 3.12.13 下重放时，Event Pack、情景、种子、运行和结果哈希全部匹配。导出中未发现 API Key、密码、认证 Cookie 或完整上传原文。

这证明以下核心能力已经真实存在：

- 前后端和数据库链路可用；
- 匹配随机种子实验可运行；
- 确定性规则仿真可复现；
- 历史实验不会被新实验覆盖；
- 可复现导出不是演示占位功能。

### 2.2 LLM 认知实验

随后执行了智谱 `glm-5.2` 的 `HYBRID_LLM` 实验：

- 实验 ID：`exp-e297228fe18b4809`；
- 计划模型调用：12 次；
- 实际模型调用：12 次；
- 总 token：97,474；
- 其中输入 60,689、输出 36,785、缓存 35,008；
- 记录费用上界约 0.252582 美元；
- 认知准备阶段约 9 分钟 10 秒；
- 仿真阶段约 7 秒；
- 最终模式：`HYBRID_LLM_PARTIAL_RULE_FALLBACK`；
- 12 次中 6 次发生规则回退；
- 回退原因在 `MODEL_RESPONSE_INVALID` 与 `SCHEMA_INVALID` 之间交替；
- 6 次通过结构校验的认知结果最终没有形成认知信号影响订单，`cognitiveOrderCount=0`。

这证明“LLM 接口被真实调用”已经成立，但“LLM 对最终市场机制产生了可解释、可验证的实际影响”尚未成立。

### 2.3 结果解释助手

结果解释助手在智谱思考模式开启时连续返回 `MODEL_RESPONSE_INVALID`；切换模型也未恢复。关闭思考模式后，`glm-4.5-air` 成功返回、流式展示、Markdown 排版、证据编号、会话保存和多轮追问均可用。

成功回复的排版和证据交互良好，但内容出现多处统计和机制误读，详见第 9 节。

### 2.4 当前生产运维状态

2026-07-29 的只读服务器检查显示：

- Docker 与 Nginx 均为 `enabled`、`active`；
- `eventshock-app` 与 `eventshock-caddy` 容器均处于健康状态；
- 宝塔存在且仅显示一条“EventShock GitHub 自动同步部署”任务；
- 任务周期为每 10 分钟；
- 同步日志持续报告运行版本、容器和公网健康保持在同一提交；
- 当前部署提交为 `b97018eaa5212548709047a88d8242b14b084c8d`。

因此，历史上的重启后 Nginx 不可用问题当前不属于正在发生的故障，但必须保留为运维回归项。

---

## 3. P0 总览

| ID | 问题 | 当前状态 | 首要修复 |
| --- | --- | --- | --- |
| P0-01 | AI 引导一次不确定失败后永久锁住当前工作流版本 | 源码已确认 | 增加可审计的恢复、放弃和重新调用协议 |
| P0-02 | 全局思考模式直接进入严格 JSON 工作流，导致智谱持续返回不可用结构 | 当前已复现 | 严格结构化工作流默认强制关闭思考，并按工作流验证能力 |
| P0-03 | 运行前检查可显示“有效”，同时明确说不会调用所选 LLM | 当前已复现 | 将“按规则降级可运行”和“混合 LLM 可运行”拆成两个有效状态 |
| P0-04 | LLM 实验真实调用 12 次，但 6 次回退且没有认知信号影响订单 | 当前已复现 | 为有效信号到确定性意图建立可审计桥接和作用率门禁 |
| P0-05 | 解释助手产生统计上错误或证据不支持的结论 | 当前已复现 | 在生成后增加确定性统计事实校验，不通过则拒绝展示 |
| P0-06 | 曾在对话中直接提供长期邮件和管理员凭据 | 安全操作缺口 | 立即轮换相关凭据，并验证仓库、日志和导出均无残留 |
| P0-07 | 用户仍可能把产品理解为股票跟踪或个人投资建议工具 | 外部反馈已复现 | 第一屏、导航和结果结构明确机构研究用户及排除场景 |

---

## 4. AI 引导工作流

### P0-01：`UNKNOWN` 操作会永久锁住当前工作流版本

**现象**

- AI 返回 unusable response 后，用户再次发送同一内容仍失败；
- 换一条新消息也可能返回 409；
- 当前工作流无法继续，只能新建另一条引导工作流；
- 页面没有“确认未成功并重试”“放弃本次调用”“恢复到调用前版本”或“删除工作流”入口。

**源码证据**

- `backend/app/main.py` 的 `createGuidedWorkflowTurn` 在模型或提交结果发生异常时调用 `markTurnUnknown`；
- `backend/app/guided_workflow/repository.py` 的操作状态只允许 `PENDING`、`SUCCEEDED`、`UNKNOWN`；
- 同一 `workflow + owner + expectedVersion` 存在唯一索引；
- `UNKNOWN` 不会推进工作流版本，也不会释放该版本的唯一占用；
- 同一 `clientRequestId` 会被拒绝为“不允许自动再次调用”；
- 新 `clientRequestId` 又会被唯一索引拒绝为“已有操作占用当前版本”；
- 现有测试把这种锁死行为当成了预期安全行为。

**根因**

系统为了避免供应商结果不确定时重复计费，采取了正确的 fail-closed 原则，但只实现了“禁止自动重试”，没有实现“人工决定如何收尾”。安全状态机缺少终态：

- `CONFIRMED_FAILED`；
- `ABANDONED_BY_USER`；
- `RECONCILED_SUCCEEDED`；
- `RETRY_AUTHORIZED`。

**解决方案**

1. 保留 `UNKNOWN`，但给它增加独立恢复协议，不直接把 `UNKNOWN` 当永久终态；
2. 为操作保存：
   - 供应商请求 ID；
   - 是否收到 HTTP 响应；
   - 是否获得完整 usage；
   - 是否得到可解析正文；
   - 错误发生在供应商前、供应商后还是数据库提交阶段；
3. 如果确认请求根本未发出，可自动转为 `CONFIRMED_FAILED` 并释放版本；
4. 如果请求可能已计费，要求用户确认：
   - “放弃这次未知结果并允许一次新调用”；
   - 或“保留当前工作流，稍后由管理员核对供应商日志”；
5. 新调用必须使用新的幂等 ID，并在审计记录中链接原未知调用；
6. 对数据库提交阶段失败，应优先使用已缓存的模型响应完成幂等提交，不应再次调用模型；
7. 增加“关闭/归档引导工作流”，避免坏工作流永久留在列表中。

**验收标准**

- 模拟模型超时、连接中断、响应后数据库写入失败三种情况；
- 每种情况都能在不重复静默计费的前提下恢复；
- 用户不需要新建整个工作流；
- 操作历史能清楚显示原请求、人工决定和新请求；
- 同一恢复动作重复点击仍幂等；
- 不再存在当前版本永久被 `UNKNOWN` 占用的路径。

### P1-01：发送中的用户消息会从对话中消失

**现象**

用户点击发送后，输入框立即清空；在模型返回前，刚发送的消息没有稳定显示。失败时输入内容也不会自动恢复，用户会怀疑“是不是没发出去”。

**源码证据**

`frontend/src/pages/guided-workflow-page.tsx` 的 `sendMessage` 在请求开始前执行 `setMessage('')`，但没有 optimistic message 或失败恢复。

**解决方案**

- 前端建立临时消息对象，状态为 `SENDING`；
- 服务器确认后用正式消息替换；
- 失败时保留原文并显示 `FAILED`，提供“编辑后重试”和“复制”；
- 不允许失败消息静默消失；
- 页面刷新后，从服务器恢复正式记录，本地临时消息只作为短期 UI 状态。

**验收标准**

- 发送后 100 毫秒内用户消息出现在右侧；
- 请求持续 30 秒时消息仍存在；
- 请求失败时内容不丢失；
- 成功后临时消息不会重复显示。

### P1-02：生成候选后候选卡片可能突然消失

**现象**

AI 生成“待人工决定候选”后，候选卡片曾突然消失，只剩助手的通用说明。用户无法确认候选是否保存、是否应用、是否被系统丢弃。

**解决方案**

- 候选 proposal 必须是服务端持久状态；
- 在用户执行“应用”“拒绝”“替换”之前始终显示；
- 即使进入下一阶段，上一阶段候选也应在历史中只读可查；
- 为候选显示状态：待审核、已应用、已拒绝、已被新候选替代；
- 状态变化必须有时间和审计来源。

**验收标准**

- 刷新页面后待审核候选仍存在；
- 多标签页操作时给出版本冲突而不是隐藏候选；
- 应用后可查看应用前后的字段差异。

### P1-03：阶段前进后仍显示上一阶段的助手回复

**现象**

用户点击“我已人工检查，继续到第二步”后，阶段标识进入下一步，但助手仍停留在上一阶段“标题、摘要或研究问题是否需要调整”的回复。用户无法判断下一步应该做什么。

**根因**

阶段推进与生成阶段引导消息是两个独立动作；当前控制器推进阶段，但没有生成或插入一个新的确定性阶段开场消息。

**解决方案**

- 每个阶段定义一条确定性的双语开场消息；
- 阶段推进成功时由后端原子插入；
- 开场消息说明：
  - 当前阶段目的；
  - 本阶段需要用户提供什么；
  - AI 可以做什么；
  - 哪一步必须人工确认；
- 不依赖第二次 LLM 调用来生成阶段说明，避免额外费用和失败点。

**验收标准**

- 每次阶段变化后最末助手消息与当前阶段一致；
- 刷新后保持一致；
- 不会出现阶段为第二步、问题仍属于第一步的组合。

### P1-04：生成期间缺少可理解的进度状态

**现象**

用户只看到页面停住，不知道 AI 是否在读取输入、检查隐私、生成结构、执行本地验证或已经失败。

**解决方案**

使用真实后端阶段事件，而不是假进度动画：

1. 正在检查输入安全；
2. 正在准备受限上下文；
3. 正在等待模型返回；
4. 正在校验结构；
5. 正在检查允许字段；
6. 正在保存候选；
7. 正在等待人工审核。

超出阶段正常耗时后显示“仍在等待供应商”，并显示已经等待的秒数。

**验收标准**

- 状态变化必须来自真实事件；
- 断线重连后可恢复最新状态；
- 失败时保留最后成功阶段和可操作建议。

### P1-05：“下一步”无法点击时，字段定位仍不充分

**现象**

测试者点击下一步后页面没有明显变化，不知道缺少哪个字段。此前已经提出“应标红并定位需要填写的地方”。

**当前进展**

当前 Factory 代码已存在错误摘要、红色区段和焦点跳转；AI 引导也增加了一部分 `guidedAdvanceBlocker`。因此该问题应标为“已有改善但需要整条链路回归”，不能直接视为完全解决。

**解决方案**

- 所有阻止推进的校验统一返回结构化字段：
  - `code`；
  - `sectionId`；
  - `fieldId`；
  - 双语用户说明；
  - 推荐动作；
- 页面顶部显示错误摘要；
- 对应控件红框、内联说明；
- 自动滚动并聚焦第一个错误；
- 每修复一项，摘要实时减少；
- 不把 F12 控制台当作用户诊断入口。

**验收标准**

- 人为清空每个必填字段并点击继续；
- 用户无需打开开发者工具即可定位；
- 键盘用户能直接获得焦点；
- 中英文都不显示内部异常堆栈。

### P2-01：AI 引导与专家页面之间的职责边界不够明确

**现象**

用户问“为什么不在 AI 引导里面做”。当前 AI 引导只生成并移交可编辑草稿，真实来源审核、冻结、场景验证和提交仍在专业页面完成，但这种设计理由没有被讲清楚。

**解决方案**

- 在 AI 引导中显示完整流程地图；
- 对必须跳出的阶段给出明确原因：
  - 来源审核涉及证据授权；
  - claim 审核是不可由 AI 代替的人类判断；
  - 冻结会锁定可复现输入；
  - 启动实验涉及费用和解释责任；
- 跳转后保留“返回 AI 引导”入口；
- AI 引导读取已完成的人类状态，并在返回时继续下一步，而不是让用户重复描述。

---

## 5. Event Pack Factory 与事件包审核

### P1-06：规则回退抽取会产生大量碎片化 claim

**现象**

规则回退从一份 FAA 文本中抽取出 16 条候选，其中包括：

- 文档标题；
- URL；
- 日期；
- 被换行截断的半句话；
- 相邻句子的碎片；
- 每条固定 65% 置信度；
- 几乎相同的影响通道。

**影响**

- 人工审核负担增加；
- 批量批准诱导用户把格式碎片当事实；
- 后续机制映射失去意义；
- “AI 抽取”看起来像简单逐行切割。

**解决方案**

- 规则回退先做结构化预处理：
  - 删除导航、标题重复、URL 独立行和纯日期行；
  - 合并跨行句子；
  - 按句法终止符切分；
  - 合并共指或同一命题的连续句；
- 对 claim 做最小完整性检查：主语、谓语、时间或作用对象至少满足一组模板；
- 回退模式必须显著标记“低质量候选，仅用于不中断流程”；
- 回退 claim 默认不允许批量批准；
- 提供“重新用 LLM 抽取”而不是让用户逐条处理低质量碎片。

**验收标准**

- FAA 测试文本不再产生 URL、标题、日期独立 claim；
- 不出现半句话；
- 重复命题可检测；
- 回退质量测试加入固定夹具。

### P1-07：LLM 抽取的置信度与影响通道过度一致

**现象**

`glm-5.2` 抽取质量明显优于规则回退，但 11 条 claim 几乎全部为 99%，且全部同时映射到：

`belief, liquidity, passiveFlow, stopLoss`

**问题**

- 99% 容易被误读为事实正确概率；
- 每条 claim 都命中所有通道，说明通道没有区分价值；
- 发布时间、适用范围、飞行禁令、风险描述不应自动拥有完全相同的市场传播机制。

**解决方案**

- 把单一 `confidence` 拆成：
  - 抽取文本是否忠实；
  - 来源等级；
  - 时间边界确定性；
  - 影响通道是否为推断；
- 不允许模型把“来源官方”直接等价为“所有字段 99%”；
- 每个通道要求短理由和证据类型；
- 影响通道可多选，但默认最多两个；超过两个必须说明；
- 由本地规则校验明显不合适的通道组合；
- 前端明确写“这是抽取置信度，不是事件发生概率或市场影响概率”。

**验收标准**

- 不同 claim 的通道能体现语义差异；
- 置信度不再机械相同；
- 用户可查看并编辑通道理由；
- 统计监控模型输出的置信度熵与通道分布。

### P1-08：影响通道名称对非专业用户不友好

**现象**

用户明确指出“需要抽取的影响通道”选项难以理解。审核页还直接显示英文内部值。

**解决方案**

| 内部值 | 中文名称 | 页面解释示例 |
| --- | --- | --- |
| `belief` | 认知与预期变化 | 信息改变参与者对事件严重程度或未来状态的判断 |
| `liquidity` | 流动性供给变化 | 做市、挂单深度或买卖价差可能发生变化 |
| `passiveFlow` | 被动资金流 | 指数、ETF 或规则化组合产生非自主买卖 |
| `stopLoss` | 止损与强平 | 价格或风险阈值触发规则化卖出、减仓或平仓 |

每个选项应显示：

- 中文名称；
- 一句话解释；
- 一个与当前事件相关的例子；
- “事实”还是“机制假设”；
- 对应仿真参数；
- 不选择时的含义。

**验收标准**

- 中文界面不出现裸 `passiveFlow`；
- 用户能在不查文档的情况下解释四个选项的区别；
- 影响通道与情景构建器中的机制名称一致。

### P1-09：拒绝来源是不可逆的，但 UI 没有充分说明

**现象**

用户拒绝搜索结果后希望“反悔”。当前按钮只有“拒绝”，没有确认和撤销。

**源码证据**

`backend/app/event_pack_factory/repository.py` 在拒绝时同一事务物理删除来源原文；随后从 `REJECTED` 改回 `APPROVED` 会明确失败。

**根因**

后端把“本轮不采用”与“撤回原文处理授权并永久删除”合并成一个动作。

**解决方案**

将动作拆为两层：

1. **排除出本次证据集**：可逆，仅改变选择状态；
2. **永久删除来源原文**：不可逆，单独确认并解释影响。

如果继续坚持“拒绝即删除”的隐私策略，则至少需要：

- 拒绝前强确认；
- 明确写“原文将立即物理删除，不能撤销”；
- 提供“重新添加同一来源”的快捷入口；
- 不应把按钮只写成普通“拒绝”。

**验收标准**

- 用户在不可逆动作前能准确复述后果；
- 普通排除可以撤销；
- 永久删除后无法从 API、日志和数据库恢复原文；
- 审计区分排除与删除。

### P1-10：搜索线索、Reader 正文和正式证据的身份容易混淆

**现象**

用户反复询问“之前的都不通过吗”“是不是之前都要拒绝”。搜索结果的可见卡片让人误以为它们已经是正式证据。

**当前设计**

搜索摘要只能作为 `DISCOVERY_ONLY`，批准它只代表允许 Reader 获取全文；Reader 得到的正文要作为新来源再次审核。

**解决方案**

在 UI 中显示一条不可省略的来源转换链：

```text
搜索线索
  → 人工决定是否读取
  → Reader 获取正文
  → 新的候选证据
  → 人工核对原网页
  → 正式批准
```

- 搜索线索卡片不使用与正式证据相同的“批准”词；
- 改成“允许读取全文”；
- 正式来源用新的卡片和新 ID；
- 明确旧搜索线索无需为了进入下一步而全部拒绝；
- 提供状态筛选：线索、等待读取、待审核证据、已批准证据、已排除。

### P1-11：安全扫描只报告分类，没有足够的处理建议

**现象**

页面显示 `EMAIL_ADDRESS`、`PHONE_NUMBER` 和 `REVIEW`，但用户不知道：

- 命中的是公开机构联系方式还是不必要个人信息；
- 是否阻止后续操作；
- 应该如何脱敏；
- 是否仍会发送给模型。

**解决方案**

- 显示字段位置和风险类型，但继续不显示敏感原文；
- 区分：
  - 明确秘密；
  - 高风险身份信息；
  - 公开机构联系方式；
  - 仅需人工确认的普通匹配；
- 给出动作：
  - 自动遮蔽后继续；
  - 打开原文编辑；
  - 删除来源；
  - 查看数据处理说明；
- 在发送模型前显示最终被保留/移除的字段计数；
- 不把普通公开机构邮箱写成与 API Key 同等级的风险。

### P2-02：“重新抽取”发现性不足

**现象**

按钮实际存在于事件包标题区域，但用户在长页面中找不到，并认为没有重新抽取功能。

**解决方案**

- 在抽取模式卡片旁增加“重新抽取”；
- 当规则回退或 LLM abstain 时显示高优先级行动通知；
- 审核队列顶部提供同一入口；
- 滚动后使用粘性审核工具栏保留“重新抽取”和待审核数量。

### P2-03：抽取模式长字符串溢出

**现象**

`ZHIPU_glm-5.2_ABSTAINED_RULE_FALLBACK` 曾溢出摘要卡片。

**当前代码状态**

摘要栅格已改成自适应列，但 `.pack-summary strong` 仍需要明确的 `min-width: 0` 和换行策略，不能依赖偶然布局。

**解决方案**

- 普通用户显示本地化短名称，如“智谱 GLM-5.2 未产出，已用规则回退”；
- 完整内部模式放在可复制技术详情中；
- 卡片强制 `min-width: 0; overflow-wrap: anywhere; word-break: break-word`；
- 为小宽度和中文测试快照。

### P2-04：批量批准虽然有警告，但仍需按抽取质量分级

**当前能力**

已经存在“一键批准全部待审核项”和二次风险确认。

**问题**

不同抽取模式的风险不同。对规则回退碎片、低置信度、涉及多个影响通道或内容安全为 `REVIEW` 的 claim，不应允许同等强度的一键批准。

**解决方案**

- 仅对满足质量门槛的候选开放批量批准；
- 把异常 claim 自动从批量集合中排除；
- 二次确认显示：
  - 将批准多少条；
  - 有多少条低置信度；
  - 有多少条机制映射仍是推断；
  - 有多少条来自非官方来源；
- 批量批准后仍允许在冻结前撤销单条决定。

---

## 6. 情景构建器与运行前检查

### P1-12：被拒绝的澄清 claim 会让澄清延迟干预失效，但错误没有直接指向修复

**现象**

验证返回：

`INTERVENTION_MECHANISM_DISABLED`

具体原因是澄清 claim 已被拒绝，而情景仍选择 `clarificationDelay`。页面多次点击“更新”“验证”后看起来没有变化，用户不知道必须换干预变量或恢复相应 claim。

**解决方案**

- 将错误翻译成当前页面动作：
  - “当前 Event Pack 没有已批准的澄清信息，因此不能测试澄清延迟”；
- 高亮“选择一个干预变量”；
- 提供两个明确按钮：
  - “改用做市能力”；
  - “返回审核证据并补充澄清 claim”；
- 显示错误所依赖的 claim ID，但放在技术详情而非正文；
- 验证失败后自动聚焦干预区域。

**验收标准**

- 用户无需阅读 JSON 即可修复；
- 修改变量后错误立即消失；
- 旧错误不会因缓存继续显示。

### P1-13：保存草稿、更新当前场景、冻结场景和验证情景的状态关系不清楚

**现象**

用户修改参数后点击“更新当前场景”，再点击验证，页面仍像使用旧值。有时“更新当前场景”不可用，需要“另存为新场景”，但 UI 没说明原因。

**可能根因**

- 当前场景已冻结或被实验引用，不能原地修改；
- 表单是本地草稿，验证使用的是已保存服务器版本；
- 更新成功后的服务器版本没有明显反馈；
- 选择的场景与编辑中的草稿不是同一个对象。

**解决方案**

- 页面同时显示：
  - 正在编辑：本地草稿；
  - 当前服务器场景：名称、版本、状态；
  - 是否有未保存修改；
- “验证草稿”和“验证已保存场景”只保留一个明确语义；
- 如果必须先保存，验证按钮写成“保存并验证”；
- 已冻结或已被实验使用时，按钮写成“另存为新版本”，并解释不可原地修改的复现性原因；
- 保存成功后显示新场景 ID、版本和时间；
- 验证结果绑定明确的场景版本。

### P0-03：运行前页面的“有效”与 LLM 费用门禁发生语义矛盾

**现象**

当费用上限为 3 美元时，页面顶部显示“有效”，但 `LLM_COST_CONTROL` 明确说明：

> 当前上限低于一次响应加一次修复的最坏预留，运行时不会向模型发送请求。

这意味着用户选择了 `HYBRID_LLM`，页面却允许以完全不调用 LLM 的方式启动。

**源码证据**

`backend/app/service.py` 在 `fallbackToRules=true` 时把费用不足记为 `WARN` 而不是 `FAIL`；返回值 `valid` 只取决于 `errors` 是否为空。

**问题**

“仿真可以运行”与“用户选择的混合 LLM 方案可以运行”被压缩成一个布尔值。

**解决方案**

返回两个独立状态：

- `simulationRunnable`；
- `requestedCognitionRunnable`。

前端状态分成：

- “混合 LLM 可按配置运行”；
- “只能按规则降级运行”；
- “完全不可运行”。

当只能降级时：

- 主按钮不能继续写“创建并启动实验”；
- 应写“确认改为规则模式并启动”；
- 用户必须二次确认；
- 保存后的 scenario mode 应明确改成 `RULE_ONLY`，不能仍显示 `HYBRID_LLM`。

**验收标准**

- 3 美元场景不会被标为混合 LLM 有效；
- 10 美元场景通过费用门禁；
- 任何降级都需要显式确认并在 manifest 中如实记录。

### P1-14：最坏费用预留与实际费用差距巨大，说明不足

**现象**

运行前显示单次调用最坏预留约 8.057344 美元，而实际 12 次调用总费用上界约 0.252582 美元。用户很难判断自己实际可能花多少钱。

**解决方案**

同时显示三种数值：

1. **预计费用区间**：根据本模型最近真实 usage 或保守 token 估计；
2. **单次安全预留**：用于 fail-closed 的技术上界；
3. **用户责任上限**：绝不会超过的硬限制。

必须解释三者用途，不能只显示最大值。预留策略应避免把“一次响应的完整最大上下文 × 所有重试”机械地当作日常预计费用。

### P1-15：运行前检查中仍有大量内部枚举和英文

**现象**

中文界面仍显示：

- `HYBRID_LLM`；
- `VERIFIED_UPPER_BOUND`；
- `MECHANISM_DEMONSTRATION_NOT_FORECAST`；
- 英文系统检查名称和英文说明。

**解决方案**

- 用户层全部本地化；
- 原始枚举进入折叠“技术详情”；
- 检查按“证据”“实验设计”“模型调用”“可复现性”分组；
- 失败和警告先显示动作，再显示技术代码。

### P1-16：非 SpaceX 事件仍显示 SpaceX 局限文本

**现象**

阿拉斯加航空/波音实验的运行前限制中出现：

> 不能当作 SpaceX 或任何真实证券的价格预测。

**源码证据**

`frontend/src/pages/preflight-page.tsx` 存在硬编码 SpaceX 文案。

**解决方案**

- 改成与具体事件无关的通用表述；
- 如需事件名称，从当前 Event Pack 注入；
- 加入非 SpaceX 测试夹具，禁止跨案例品牌残留。

### P2-05：停止规则在最小值等于最大值时仍被解释成“提前停止”

**现象**

本轮最少 10 对、最多 10 对。系统记录 `TARGET_CI_HALF_WIDTH_REACHED`，解释助手随后声称“不是因为样本耗尽”。在 min=max 时，两种条件同一步发生，不能据此声称提前停止。

**解决方案**

- 停止原因允许多值；
- 记录各条件满足的具体顺序；
- min=max 时显示“完成预设 10 对；目标区间条件同时满足”；
- 解释助手不得把该状态写成节省了样本。

---

## 7. LLM 认知 Agent 与仿真运行

### P0-02：结构化工作流不应直接继承全局思考模式

**现象**

- 结果解释助手在智谱思考模式开启时连续 `MODEL_RESPONSE_INVALID`；
- 切换模型仍失败；
- 关闭思考后立即恢复；
- 本轮混合认知实验也出现大量 `MODEL_RESPONSE_INVALID` 和 `SCHEMA_INVALID`。

**源码证据**

- `backend/app/cognition/service.py` 的 `_buildRequest` 默认继承 `runtime.thinkingEnabled`；
- 结果解释 answer 请求没有覆盖该值；
- 智谱网关在支持思考的模型上直接发送 `thinking.enabled`；
- 严格输出仍要求最终只返回一个固定 JSON 对象。

**根因**

“模型支持思考”只代表供应商接受该参数，不代表：

- 思考内容能与 JSON Schema 共存；
- 流式增量能被当前解析器正确分离；
- 该模型在本项目提示词和最大 token 下能稳定完成结构化输出。

连接测试只验证了一个简单结构化响应，未覆盖长上下文、流式、引用集合、分析摘要和严格本地校验。

**解决方案**

1. 配置能力从“模型级”升级为“工作流级”：
   - 普通聊天；
   - 严格结构化抽取；
   - 严格结构化认知；
   - 严格结构化结果解释；
2. 在没有真实回归证据前，严格结构化工作流默认强制关闭思考；
3. UI 显示：
   - “全局偏好：开启”；
   - “本工作流：为保证 JSON 稳定性已关闭”；
4. 修复请求必须强制关闭思考，避免用相同失败模式重复一次；
5. 为每个供应商/模型建立真实 Key 的合约测试记录：
   - 长上下文；
   - 流式；
   - Schema；
   - thinking on/off；
   - usage；
   - repair；
6. 只有通过组合测试的模型才允许开启。

**验收标准**

- 本轮同一结果、同一模型连续 20 次结构化解释成功率满足发布门槛；
- thinking on 不会把 reasoning 内容混入 JSON；
- 不支持组合时 UI 直接禁用，而不是运行后报错；
- 模型切换时重新计算工作流能力。

### P0-04：LLM 被调用，但没有形成可观测的市场作用

**现象**

本轮混合实验：

- 12 次 LLM 调用；
- 6 次有效结构化结果；
- 6 次规则回退；
- `cognitiveOrderCount=0`；
- 规则与 LLM 实验的主要结果几乎相同。

**问题**

“LLM 不直接下单”是正确的安全边界，但系统仍应把有效的信念/行动偏好传给确定性策略。如果所有有效信号最终都没有改变任何确定性行动，则不能把这次实验描述成“LLM-based agent 参与了市场行为”。

**解决方案**

- 为每个认知决策记录完整作用链：

```text
模型输出
  → Schema 通过/回退
  → 信念变化
  → 行动偏好
  → 确定性策略接收
  → 是否改变意图
  → 风控审批
  → 订单/不下单
```

- 区分以下指标：
  - `validCognitionDecisionCount`；
  - `cognitionSignalConsumedCount`；
  - `cognitionChangedIntentCount`；
  - `cognitionInfluencedOrderCount`；
  - `cognitionBlockedByRiskCount`；
  - `cognitionNoActionCount`；
- 如果有效信号作用率为零，在运行中心和结果页显示醒目警告；
- 对代表性 Agent、决策间隔和信号强度进行最小有效性校准；
- 增加“关闭 LLM 后结果应改变/不改变”的预注册消融。

**验收标准**

- 至少能解释每一条有效信号为什么没有产生订单；
- 不能只用“LLM 不直接下单”掩盖信号未被消费；
- 混合实验报告真实作用率；
- 作用率为零时不声称 LLM 改变了市场结果。

### P1-17：认知准备阶段长时间无进度

**现象**

日志在 17:39 显示“准备确定性规则或冻结认知信号”，直到 17:48 才开始第一对路径。约 9 分钟内没有每次模型调用的可视状态。

**解决方案**

- 后端为认知阶段发送 SSE：
  - 第几轮；
  - 第几个代表 Agent；
  - 正在请求/流式接收/校验/修复/回退；
  - 已用 token；
  - 当前费用；
  - 已完成调用数/预算；
- 不显示私有思维内容；
- 对超时和限流给出明确状态；
- 提供“取消剩余 LLM 调用并按规则完成”；
- 取消必须保留已生成的冻结决策和费用审计。

### P1-18：6/12 规则回退比例过高

**现象**

50% 认知请求回退，说明模型、提示词、Schema、token 或解析组合不稳定。

**解决方案**

- 保存脱敏后的失败阶段统计，不保存可能含敏感数据的完整供应商正文；
- 分开统计：
  - 空正文；
  - 被截断；
  - 非 JSON；
  - JSON 解析失败；
  - Schema 字段错误；
  - 引用越界；
  - 动作越界；
- 对每类错误设计不同修复提示；
- 相同错误连续出现时停止继续烧预算；
- 发布门槛应包含结构化成功率，而不仅是“允许回退后实验完成”。

### P1-19：冻结认知 pilot 的实验含义不够清楚

**现象**

当前 LLM 观察聚合路径并生成冻结认知序列，不会针对每个随机种子和每条实时路径重新适应。普通用户容易把它理解为实时 LLM Agent 在每一步观察市场。

**解决方案**

- 将模式命名为“冻结代表性认知 pilot”；
- 明确显示：
  - LLM 何时被调用；
  - 看到了什么聚合数据；
  - 生成多少轮；
  - 哪些路径复用了冻结决策；
- 与未来“实时闭环认知”模式严格区分；
- 结果解释不得使用“实时观察并决策”等词。

### P2-06：已完成实验的“当前随机种子”仍显示“暂无数据”

**现象**

实验已经 10/10 完成，但运行中心顶部的当前随机种子仍为暂无数据。

**解决方案**

- 运行中显示当前种子；
- 完成后显示“最后完成种子”；
- 未运行时才显示暂无数据；
- API 明确区分 `currentSeed` 与 `lastCompletedSeed`。

### P2-07：Worker 日志在中文页面仍为英文

**解决方案**

- 后端发送稳定事件代码和结构化参数；
- 前端双语翻译；
- 原始英文日志进入技术详情；
- 不在后端把最终显示文本硬编码成英文。

### P2-08：黑色参数提示框在失去 hover 后仍残留

**现象**

Safari 中鼠标移开后 Definition Tooltip 偶尔仍停留在页面上。

**当前实现**

`ParameterHelp` 使用 Carbon `DefinitionTooltip`、`openOnHover`。该组件同时支持焦点状态，因此可能在鼠标离开后因触发器仍获得焦点而保持打开。

**解决方案**

- 明确制定交互规则：
  - hover 打开，mouseleave 关闭；
  - 键盘 focus 打开，blur 关闭；
  - 点击打开时支持再次点击、外部点击和 Escape 关闭；
- 滚动、路由变化、数据刷新时关闭；
- 同一时间只允许一个 tooltip；
- 评估改用受控 Popover，而不是依赖混合 hover/focus 的默认状态。

**验收标准**

- Safari 和 Chrome 均测试；
- 鼠标离开不会残留；
- 键盘用户仍能阅读；
- Escape 和外部点击有效。

---

## 8. 实验结果页面

### P1-20：基准、干预和差值使用不同统计量，但页面像在做普通减法

**现象**

某些卡片看起来出现：

```text
基准中位数 A
干预中位数 B
差值 C
```

但 `C` 不一定等于 `B - A`。

**源码证据**

- 基准值是所有基准路径的中位数；
- 干预值是所有干预路径的中位数；
- 差值是每对种子先做 `intervention - baseline`，再取配对差值中位数；
- “中位数之差”不必等于“差值的中位数”。

**解决方案**

- 把“差值”改为“配对差值中位数”；
- 在卡片中写明公式；
- hover 说明当前三个估计量不同；
- 需要普通减法时另显示“组中位数之差”，但不能混用；
- 图表和解释助手统一使用同一术语。

### P1-21：主要指标过多，初学者无法判断先看什么

**现象**

结果页一次展示大量指标卡片，很多为 0 或无明显变化。最强结果“最低市场深度下降”没有自动获得优先级。

**解决方案**

默认展示三层：

1. 预注册主要指标；
2. 区间不跨零或方向一致率高的关键变化；
3. 其余诊断指标折叠。

提供筛选：

- 主要指标；
- 有变化；
- 区间不跨零；
- 认知相关；
- 市场微观结构；
- 风控和账本；
- 全部。

零变化指标合并为“未观察到差异的诊断项”，而不是占据同等视觉面积。

### P1-22：结果页面没有自动突出最强证据

本轮最清晰结果是最低市场深度：

- 配对中位差约 `-15`；
- 经验区间约 `[-28, -7]`；
- 方向一致率 100%。

解释助手却优先讨论区间跨零的峰值价差和最大回撤。

**解决方案**

- 后端生成确定性的“最强支持结果”排序；
- 排序依据透明：
  - 是否为预注册指标；
  - 有效样本；
  - 区间是否跨零；
  - 方向一致率；
  - 缺失和稳定性；
- LLM 只能在排序结果内做语言解释，不能自行忽略更强结果。

### P2-09：单位显示混杂内部值

**现象**

中文页面出现 `count`、`cents`、`单位` 等不一致形式。

**解决方案**

- 建立单位目录；
- `count` 根据指标翻译为次、笔、个、单；
- `cents` 默认换算成美元或明确显示“美分”；
- tick 转换为价格；
- 百分比、比率和基点严格区分；
- 导出保留原始单位，页面可本地化展示。

### P2-10：随机种子轴标签太长

**解决方案**

- 图表显示短序号“种子 1…10”；
- tooltip 显示完整种子；
- 提供复制；
- 不把长整数当连续时间轴。

### P2-11：`DETERMINISTIC_TEMPLATE` 等内部标签出现在用户层

**解决方案**

- 用户显示“确定性结果摘要”；
- Schema 版本和生成器 ID 放入“版本与来源”；
- 技术枚举不作为标题或主标签。

### P2-12：“内部因果对比”表述仍可能夸大

**问题**

项目只在合成模型内部做受控单变量对比。即使叫“内部因果”，非专业用户仍可能理解为对真实市场存在因果证明。

**解决方案**

统一改为：

> 合成模型内的受控情景对比；不能证明现实世界因果关系。

只有在相邻位置同时出现“模型内”和“非现实因果验证”时才使用“因果”一词。

---

## 9. 结果解释助手

### P0-05：生成内容通过结构校验，但统计结论仍可能错误

**本轮错误 1：把不显著结果写成“显著扩大”**

回复写“最大报价价差显著扩大”，同时给出的区间为 `[-5.929525, 2.233987]`，明显跨零。这里的“显著”不受数据支持。

**本轮错误 2：遗漏最强结果**

最低市场深度的区间不跨零、方向一致率 100%，但第一轮解释没有把它放在核心发现。

**本轮错误 3：方向一致率写错**

- 峰值价差页面数据为 80%，回复写 70%；
- 最大回撤页面数据为 70%，回复写 60%。

**本轮错误 4：把差值解释成原始值**

回复把 `minDepth` 的负配对差值描述为“最低市场深度为负值”。实际含义是干预比基准少，不代表订单簿深度本身为负。

**本轮错误 5：否认已经提供的区间**

第二轮回复先说“未提供直接统计区间”，随后又使用已有差值范围，说明模型没有稳定区分 summary 与 paired deltas。

**本轮错误 6：混淆不同验证**

把相同随机种子的负对照与参数恢复 knockout 混为一谈。

**本轮错误 7：对停止原因做无依据推断**

声称不是因样本耗尽停止，但本轮最小和最大均为 10。

**本轮错误 8：认知轮数推断错误**

回复称 4 轮，但 4 个代表 Agent、12 次调用对应 3 个完整决策轮。

**本轮错误 9：错误解释认知订单为零**

“LLM 没有直接下单”是系统永远成立的安全边界，不是 `cognitiveOrderCount=0` 的充分解释。真正需要解释的是有效信号为何没有改变确定性订单意图。

**根因**

当前校验重点是：

- JSON Schema；
- 引用 ID 白名单；
- 投资建议禁词；
- 引用集合一致性。

它没有验证语言结论与数值之间的关系。

**解决方案**

在 LLM 后增加确定性语义校验器：

- 出现“显著”“明确”“可靠”时，检查预注册显著性规则；
- 提到方向一致率时，数字必须与工具输出一致；
- 提到区间时，端点和是否跨零必须一致；
- 区分原始值、配对差值和区间；
- 提到最强结果时必须覆盖后端排序的必选项；
- 停止原因只能来自结构化停止事件；
- 认知轮数、调用数、回退数通过公式校验；
- 未通过时：
  1. 用精确错误生成一次修复请求；
  2. 修复仍失败则不展示模型答案；
  3. 回退到确定性模板摘要。

**验收标准**

- 为上述九种错误分别建立固定单元测试；
- 错误回复不能进入会话历史；
- 用户不会看到“结构正确但事实错误”的最终答案；
- 确定性回退仍可读且保留引用。

### P1-23：证据完整性警告存在误报

**现象**

正文引用的 6 个证据都能在引用集合和工具活动中找到，页面仍显示“部分证据引用无法完整核对”。

**可能根因**

系统把“工具读取过但回答没有引用”的切片也计入完整性问题。读取未引用不等于证据不一致。

**源码位置**

`frontend/src/components/result-interpretation-content.tsx` 同时处理：

- 正文引用；
- grounding 集合；
- 工具活动；
- `unreferencedToolActivity`。

**解决方案**

只在以下情况报警：

- 正文引用不在 grounding 集合；
- grounding 集合引用未出现在正文或摘要；
- 引用 ID 不在允许集合；
- 工具类型与证据定义不匹配；
- 引用的区段不存在或为空。

“已读取但未引用”只放技术详情，不应触发黄色完整性警告。

### P1-24：分析摘要标题存在，但内容价值不稳定

**现象**

有时只看到“分析摘要（不是思维链）”和完整性警告，摘要本身不明显或没有给出真正的检查清单。

**解决方案**

- 分析摘要固定为短结构：
  - 读取了哪些区段；
  - 检查了哪些数值关系；
  - 哪些数据缺失；
  - 哪些结论不能下；
- 空摘要不显示 disclosure；
- 禁止把正文重复一遍；
- 不显示供应商私有思维链。

### P1-25：解释输入过大、速度慢、费用高，但准确性没有同步提高

**现象**

- 第一轮约 16,218 tokens、30.3 秒；
- 第二轮约 10,288 tokens、36 秒；
- 仍出现统计错误。

**解决方案**

- 首轮不必把所有 11 类证据完整塞给模型；
- 由确定性摘要层先生成：
  - 研究问题；
  - 干预；
  - 主要指标；
  - strongest findings；
  - 关键局限；
  - 认知作用摘要；
- 模型按问题调用有限工具；
- 长数组只传 top-k、聚合和可请求游标；
- 复用稳定的上下文哈希和供应商缓存；
- 把“减少 token”与“保持证据完整性”一起测试。

### P1-26：后续建议可能要求用户做尚未运行的实验，却没有标明

**现象**

建议包括“如果增加到 50/100 对会怎样”。当前结果无法回答，只能新建实验。

**解决方案**

- 建议分成：
  - 可直接用当前结果回答；
  - 需要新实验；
  - 需要外部数据；
- 对需要新实验的建议显示“创建新实验草稿”；
- 不让用户误以为继续聊天能凭空获得新模拟结果。

### P1-27：错误信息仍过于笼统

**现象**

页面只显示 `MODEL_RESPONSE_INVALID` 和“检查配置或稍后再试”。实际根因可能是 thinking、截断、非 JSON、Schema 字段、引用不匹配等。

**解决方案**

用户层错误应包含：

- 失败阶段；
- 是否已经产生费用；
- 是否执行修复；
- 最终失败类别；
- 推荐动作；
- 追踪 ID。

例如：

> 模型返回了内容，但开启思考后没有形成可解析的最终 JSON。系统已关闭思考重试一次，仍失败；本次未保存不完整回复。建议关闭思考或换用已通过结构化测试的模型。

### P2-13：技术详情过长

**解决方案**

- 保持默认折叠；
- 先显示异常项；
- “已读取但未引用”汇总为一行；
- 提供下载审计 JSON；
- 普通用户不直接看到内部 `result:*`。

### 已改善、必须回归的解释 UI

以下问题当前已有实现，不应重复开发，但必须保留回归测试：

- Markdown 已通过安全 GFM 组件渲染；
- 原始 `[result:*]` 已转换为 `[1]` 等证据编号；
- 用户消息整体靠右、正文左对齐；
- 助手消息靠左；
- 证据面板可定位结果区段；
- 历史会话无需迁移即可重新渲染；
- 用户问题仍按纯文本显示；
- 原始 HTML、脚本和危险链接不会执行。

---

## 10. 机制链路

### P1-28：中文页面仍暴露大量英文事件类型和情景角色

**现象**

包括：

- `intervention`；
- `baseline`；
- `scenario configuration applied`；
- `opening auction started`；
- `opening auction cleared`；
- `action intent created`；
- `volatility halt triggered`；
- `volatility halt ended`；
- `arbitrage`。

**解决方案**

- 后端继续保留稳定事件代码；
- 前端建立完整双语目录；
- 用户显示“干预组”“基准组”“情景配置已应用”等；
- 未知代码显示“未知事件（代码）”，不能静默显示英文。

### P1-29：Agent 类型没有完整汉化

包括 `deleveraging`、`forcedliquidation`、`institutionalexecution` 等。

**解决方案**

- 建立 Agent 类型目录；
- 显示名称、角色、受哪些规则约束；
- 机制链路、结果页和导出说明使用同一翻译源。

### P1-30：同一步骤大量事件没有稳定顺序

**现象**

步骤 0 出现大量观察、信念更新、意图和成交；步骤 26、27 也有多笔订单和成交，但只按 step 展示。

**解决方案**

- 每条事件增加：
  - 全局序号；
  - step；
  - step 内 phase；
  - phase 内 sequence；
  - 因果 parent ID；
- 时间线默认按稳定序号排序；
- 允许按“证据→观察→信念→意图→风控→订单→成交→指标”聚合。

### P1-31：原始 payload 字段对用户不友好

包括：

- `side`；
- `requestedQuantity`；
- `approvedQuantity`；
- `limitPriceTicks`；
- `reasonCode`；
- `tradeId`；
- `priceTicks`；
- `aggressiveSide`；
- `makerAgentId`；
- `takerAgentId`。

**解决方案**

- 默认显示本地化字段和换算值；
- tick 换算为实际合成价格；
- SELL 显示“卖出”；
- Agent ID 同时显示角色；
- 原始 payload 进入折叠技术详情。

### P1-32：订单和多笔成交没有聚合

**现象**

订单请求 22 单位，随后只看到某一笔成交 3 单位，页面没有显示累计成交和剩余数量。

**解决方案**

- 订单详情显示：
  - 请求；
  - 风控批准；
  - 累计成交；
  - 剩余；
  - 均价；
  - 成交笔数；
  - 最终状态；
- 每笔成交链接回订单；
- 订单链接到对应指标贡献。

### P1-33：注册干预与其他情景机制容易混淆

**现象**

本轮注册干预是做市能力下降，但时间线也出现 `risk-off-flow` 卖单。用户可能误以为卖单本身就是干预变量。

**解决方案**

- 清楚区分：
  - 本实验唯一注册干预；
  - Event Pack 触发机制；
  - 场景固有机制；
  - Agent 行为；
- 每个事件标注来源层；
- 只有与基准不同的字段才标“干预变化”。

---

## 11. 验证、治理与发布门禁

### P1-34：治理页使用“阻止发布”容易吓到课堂演示用户

**现象**

页面显示 P0 发布门禁和多个阻塞项，但没有先解释“可以做教学演示，不能做生产级现实声明”。

**解决方案**

将状态拆分为：

- 教学演示可用；
- 内部研究原型可用；
- 现实预测声明不可用；
- 投资决策用途禁止；
- 生产级外部验证未完成。

### P1-35：阻塞项数量多，但缺少摘要和跳转

**解决方案**

- 页面顶部显示阻塞分类；
- 每类给出负责人、证据、下一步；
- 提供跳转和筛选；
- 不把所有模型库存和检查一次铺满。

### P1-36：“已定义”与“已执行”没有足够强的视觉区分

**现象**

治理系统包含 10 类红队用例定义，但实际执行可能为 0/10。普通用户容易把“系统有测试定义”误解为“测试已通过”。

**解决方案**

- 明确区分：
  - 已定义；
  - 已自动执行；
  - 已人工执行；
  - 已通过；
  - 已失败；
  - 已过期；
- 没有执行证据时只能显示 `NOT_RUN`。

### P1-37：运行时遥测在进程重启后可能清零

**解决方案**

- 显示观察窗口开始时间；
- 不把进程内计数冒充长期指标；
- 关键安全指标持久化或导出到监控系统；
- 重启后显示“自本次启动以来”。

### P1-38：治理页缺少部署 SHA 与 CI 证据

**解决方案**

- 展示当前部署 SHA；
- 展示三项必需 CI 状态；
- 显示最近部署时间；
- 链接 GitHub commit 和 Actions；
- 服务器健康 SHA 与前端版本必须一致。

---

## 12. 历史与可复现导出

### P1-39：已登录账号仍显示“匿名浏览器会话历史”

**现象**

用户已经登录，历史页和运行中心仍显示：

> 这里只显示当前匿名浏览器会话的实验。

但当前数据库已经按账号保存实验和 AI 会话。

**源码证据**

`frontend/src/components/experiment-history.tsx` 仍硬编码匿名会话标题和说明。

**解决方案**

- 登录用户显示“当前账号的实验历史”；
- 未登录模式如果已经禁用，不应保留匿名文案；
- 留存限制按账号、全站和状态分别说明；
- 管理员历史认领迁移后显示真实所有权。

### P1-40：历史记录缺少友好检索

**现象**

列表以长技术 ID、Event Pack ID、参数和模式为主。

**解决方案**

- 显示事件标题和研究问题；
- 支持搜索、状态、日期、模型、干预参数筛选；
- 支持重命名、备注、归档；
- 管理员可看所有者，但普通用户不能看其他账号活动。

### P1-41：失败实验的导出按钮语义不清

**解决方案**

- 完成实验：导出完整可复现包；
- 失败实验：导出诊断包；
- 如果没有足够数据生成合法包，则禁用并说明；
- 文件名和清单明确区分。

### P1-42：规则模式 manifest 仍可能出现 LLM provider

**现象**

规则导出中 `llmModel=null`、调用数为 0，但 `llmProvider` 仍可能显示 `zhipu`，容易误导为使用过智谱。

**解决方案**

- 规则模式 provider/model 均为 `null`；
- 如需记录“当时配置了哪个供应商”，使用独立字段 `configuredButUnusedProvider`；
- 用户摘要明确“未调用外部模型”。

### P2-14：规则模式仍包含空 cognition 文件但缺少说明

**解决方案**

- 保持固定导出契约是合理的；
- 空文件必须包含：
  - `mode: RULE_ONLY`；
  - `reason: no external cognition requested`；
- README_REPRODUCE 说明为什么文件存在。

### P2-15：时间显示缺少时区

**解决方案**

- 页面显示用户时区；
- 技术详情显示 ISO 8601 UTC；
- 导出始终 UTC；
- 历史表头注明时区。

### P3-01：页面底部出现孤立的 `arbitrage`

**解决方案**

- 定位该文本来自事件类型、隐藏筛选值还是意外 DOM；
- 用户层翻译或删除；
- 增加页面文本快照检查，避免内部枚举漂到 footer。

---

## 13. 产品定位、目标用户与新手体验

### P0-07：目标用户虽已写在文档中，但第一眼仍不够明确

**外部证据**

Checkpoint 1 中只有 4/12 人认为项目清楚，为本轮最低；有人把它描述为“跟踪股票并给个人投资者建议”的工具。

**项目应固定的用户**

- 首要用户：资管机构、银行、交易所或类似组织中的市场事件风险分析师和研究团队；
- 次要用户：教授行为金融、市场结构、风险分析的教育者；
- 明确排除：使用输出进行个人证券买卖、择时、仓位或收益预测的用户。

**解决方案**

- 登录后第一屏直接显示：
  - 为谁构建；
  - 帮助完成什么研究任务；
  - 不用于什么；
- “AI 引导”和“专家工作区”是同一机构研究目标下的熟练度轨道，不是散户版和机构版；
- 结果页不能只靠 footer 免责声明；
- 图表水印、结果标题、解释助手开头都说明“合成模型内情景”；
- 不使用类似实时行情终端的文案；
- onboarding 问卷应推荐使用模式，但不能改变产品责任边界。

### P1-43：专业工程量没有转化成用户能理解的核心任务

**解决方案**

所有页面围绕一句核心任务组织：

> 在冻结相同事件事实和随机条件后，只改变一个市场机制，比较结果分布并追踪差异是如何产生的。

新手模式每页只突出一个主动作；高级设置折叠，但不删除。

### P1-44：研究型边界仍主要依赖文字免责声明

**解决方案**

把边界做进结构：

- 合成数据图表始终带“模拟数据”水印；
- 真实来源事实与合成市场机制分栏；
- 结果不能导出为“买入/卖出建议”；
- AI 提示词和本地校验禁止投资动作；
- 真实价格和实时行情不接入核心模拟；
- 每个结果卡显示数据版本、模型版本和有效样本。

---

## 14. 账号、隐私、安全和法律条款

### P0-06：长期凭据曾在对话中明文提供

**风险**

邮件发送凭据和管理员初始密码曾被直接粘贴到对话。即使没有写入仓库，也应视为已经暴露给不必要的处理边界。

**必须执行的操作**

1. 轮换邮件服务密码；
2. 轮换管理员密码；
3. 检查服务器应用日志、部署日志、宝塔日志和 shell history；
4. 使用 secret scanner 检查 Git 历史和当前工作区；
5. 确认新值只存在于服务器受限 secret 文件；
6. 不在任何修复文档、issue、PR、截图或测试夹具中重复原值。

**验收标准**

- 旧凭据无法使用；
- 新凭据不在 Git、日志、数据库、导出或浏览器存储；
- secret 文件权限符合部署文档；
- 邮件验证码发送仍正常。

### P1-45：使用邮箱登录意味着系统确实处理 PII

**问题**

“不提供 PII”不能被解释为系统完全不处理 PII，因为邮箱本身就是个人信息。

**解决方案**

- 明确最小必要处理：邮箱只用于账号、验证码、安全通知；
- 不把邮箱发送给 LLM；
- 管理员活动面板只显示完成安全与支持所需的信息；
- 对研究内容中的邮箱、电话继续拦截或脱敏；
- 提供数据删除、导出和保留说明；
- 明确数据库备份和日志中的邮箱处理。

### P1-46：条款文本很长不等于法律有效性已经得到保证

**解决方案**

- 保留版本化同意、旧用户重新同意和审计时间；
- 由合格法律人士进行司法辖区审查；
- 页面不得声称“已经保证法律效力”；
- 显示运营方、适用范围、生效日期、联系方式和变更机制；
- 对未成年人、教育使用和跨境数据另行评估。

### 已正确实现、需要继续保护的边界

- API Key 不保存到账号；
- API Key 不进入数据库、浏览器长期存储、日志或导出；
- AI 对话可以按账号保存在服务器数据库；
- 实验历史按账号持久化；
- 来源原文不进入可复现导出；
- 结果解释输入会进行秘密和 PII 扫描。

---

## 15. 多供应商、搜索与高级 API 参数

### P1-47：供应商“支持”与“真实通过项目测试”必须分开

**现状**

非智谱供应商已有“社区预览/未完成真实 Key 端到端验证”的警告，这是正确方向。

**解决方案**

模型目录为每个模型显示：

- 官方 API 可用；
- 项目自动合约测试通过；
- 真实 Key 端到端测试通过；
- 严格 JSON 通过；
- 流式通过；
- 思考 + JSON 通过；
- usage 与费用核算通过；
- 最近验证日期；
- 服务区域。

未验证模型不能因为“接口兼容 OpenAI”就显示为已支持。

### P1-48：模型价格和能力快照会过期

**解决方案**

- 所有价格必须注明官方来源和快照日期；
- 定期检查模型 ID、上下文、输出上限、结构化输出和价格；
- 快照过期后费用门禁 fail-closed；
- 不根据模型名称猜测能力；
- 供应商折扣、资源包、税费与官方标价分开。

### P1-49：自定义 API 参数需要安全 allowlist

**解决方案**

可允许用户配置：

- temperature；
- top_p；
- seed；
- timeout；
- 最大输出；
- 供应商明确支持的推理强度。

不能开放任意：

- base URL；
-请求头；
-工具；
-代理；
-回调地址；
-文件路径。

自定义 base URL 会带来 SSRF、凭据外泄和内部网络访问风险；若未来必须支持，应使用管理员级 allowlist 和隔离代理。

### P1-50：联网搜索安全提示与可用性需要同时改进

**问题**

过强的本地地址/域名限制会让用户不知道为什么搜索或 Reader 被拒绝；过弱又会造成 SSRF。

**解决方案**

- 保持禁止 localhost、私网、link-local、重定向到私网；
- 错误信息明确指出被阻止的是哪一类地址，不泄露内部网络细节；
- 支持公开 HTTPS 网页；
- 每次重定向重新校验；
- 搜索结果域名限制是可选研究约束，不应与 SSRF 安全边界混为一谈。

---

## 16. 通用 UI、可访问性与双语一致性

### P2-16：中文页面留白和信息密度不自然

**解决方案**

- 以中文实际行长重新校准 heading、说明和卡片间距；
- 不直接复用英文内容长度假设；
- 对中文按钮避免过宽空白；
- 长英文技术值允许换行；
- 中英文分别做视觉快照。

### P2-17：左侧导航项目宽度曾不一致

**当前状态**

当前 CSS 已统一按钮 `width: 100%` 和两列栅格，应标为“历史已改善，需回归”。

**验收标准**

- 中英文；
- 管理员和普通用户；
- 不同导航组；
- 1024、1280、1440 宽度均保持一致。

### P2-18：窗口高度较小时左侧菜单曾无法滚动

**当前状态**

当前 `.sidebar nav` 已设置 `overflow-y: auto`，属于“历史已改善，需回归”。

**验收标准**

- 高度 500、600、768 像素；
- 125% 和 200% 缩放；
- 最后一项和反馈入口可通过滚动访问；
- 键盘焦点不会滚出不可见区域。

### P2-19：卡片文字曾贴边

**解决方案**

- 建立统一卡片内边距 token；
- 通知、进度、运行详情、空状态和错误状态共用；
- 移动端最小 16px，桌面 20–24px；
- 不通过单个页面打补丁。

### P2-20：外部 IBM Plex 字体曾在 F12 中下载失败

**当前状态**

当前 `carbon.scss` 已关闭生产外部字体，主样式使用系统字体栈，应标为“历史已改善，需回归”。

**验收标准**

- 生产 Network 不请求 `1.www.s81c.com`；
- CSP 不需要字体外域；
- 字体失败不产生控制台错误；
- 中文有可靠系统字体回退。

### P2-21：错误信息应始终给出下一步

统一错误结构：

- 发生了什么；
- 是否保存；
- 是否产生费用；
- 当前数据是否安全；
- 用户能做什么；
- 若无法自行处理，提供追踪 ID 和 issue 入口。

---

## 17. 部署、Nginx、Docker 与宝塔

### 历史问题

服务器重启后曾出现网站不可访问，原因与 Nginx、Docker 和代理启动时序有关。随后项目增加了：

- systemd 依赖；
- Caddy 启动门禁；
- Nginx 注册与自愈；
- GitHub 拉取部署；
- 宝塔原生 10 分钟任务；
- 宝塔原生日志；
- 公网健康 SHA 门禁；
- 回滚。

### 当前状态

本轮只读检查表明 Docker、Nginx、两个容器和宝塔任务均正常，同步日志持续 `NO_CHANGE`。因此以下内容是回归和监控要求，不是当前故障。

### P1-51：重启恢复必须成为自动回归，不应只靠一次人工验证

**解决方案**

- 在维护窗口执行受控重启测试；
- 验证：
  - Docker；
  - Nginx；
  - Caddy；
  - app；
  - `/api/health`；
  - 登录；
  - SSE；
  - 宝塔流量；
  - 自动同步；
- 记录恢复时间和失败告警；
- 不在课堂演示前临时重启。

### P1-52：自动部署状态需要在产品治理页可见

**解决方案**

- 显示当前 SHA、GitHub main SHA、CI 状态、最近同步、最近部署、最近失败；
- 宝塔仍作为真实运维入口；
- 产品页面只做只读摘要，不提供危险部署按钮。

### P2-22：公网健康验证需要区分直接证据和同步日志证据

本轮本地环境受代理/DNS限制，未完成独立公网 curl；服务器同步日志报告公网健康一致。后续验收应同时保留：

- 外部网络直接 curl；
- 服务器内部代理链 curl；
- 宝塔 access log；
- Caddy/app health；
- release SHA。

---

## 18. 许可证与第三方内容

### P1-53：不能把 PolyForm Strict 描述成标准开源许可证

当前更准确的说法是“源码公开可见”或“source-available”。它默认不授予任意转发、修改和再分发权。

**解决方案**

- README、网站 footer、条款和 About 使用一致表述；
- 不使用“open source”误导；
- 第三方依赖仍按各自许可证保留归属；
- 用户上传来源的公开可访问性不等于再分发授权。

### P1-54：导出来源元数据不代表拥有来源再分发权

当前导出只包含来源元数据、哈希和短 claim，这是合理边界。继续要求：

- 完整原文不进入公共导出；
- 每个来源记录许可复核状态；
- 公开发布 Event Pack 前必须人工审查；
- Reader 成功抓取不能被当作许可。

---

## 19. 已解决或明显改善的问题清单

以下项目在历史对话中出现过，当前代码已有对应实现。后续不应重复宣称“尚未实现”，而应建立防回归测试：

1. 安全 Markdown 渲染；
2. `[result:*]` 内部引用转换为编号；
3. 用户消息靠右、助手消息靠左；
4. 证据面板和结果锚点；
5. 结果解释会话服务器保存；
6. 历史实验不会自动覆盖；
7. 一键批准全部 claim 的二次确认；
8. 参数说明信息图标；
9. 页面级“本页怎么做”；
10. 左侧菜单短高度滚动；
11. 左侧菜单统一宽度；
12. 生产外部字体请求关闭；
13. 多供应商目录和非智谱测试警告；
14. API Key 只在会话内存中；
15. 邮箱登录、验证码、密码重置；
16. 版本化条款同意；
17. 管理员用户面板；
18. GitHub issue 入口和模板；
19. 根 README 默认英文并链接中文；
20. GitHub 拉取式自动部署；
21. 宝塔原生计划任务和日志；
22. Nginx/Docker 重启恢复；
23. Event Pack Factory 的字段错误摘要和定位；
24. 可复现 ZIP 与重放脚本；
25. 中英文界面，默认英文。

这些功能的存在不表示体验已经完整。例如，Markdown 渲染正确不代表 AI 解释内容正确；历史实验存在不代表历史页文案和检索正确。

---

## 20. 推荐实施顺序

### 阶段 A：先修阻断与错误结论

1. 轮换已暴露凭据；
2. 修复 AI 引导 `UNKNOWN` 永久锁；
3. 严格结构化工作流默认关闭思考；
4. 拆分“仿真可运行”和“所选 LLM 可运行”；
5. 为结果解释增加确定性统计校验；
6. 为 LLM 认知增加信号作用链与作用率；
7. 第一屏明确目标用户和排除用途。

### 阶段 B：跑通新手正常链路

1. optimistic 用户消息；
2. 持久候选卡；
3. 每阶段确定性开场消息；
4. 真实进度状态；
5. 所有阻塞错误定位到字段；
6. Factory 来源状态机和可逆排除；
7. 情景草稿、保存版本、验证版本统一。

### 阶段 C：结果可读与证据可信

1. 配对估计量术语；
2. 关键指标优先级；
3. 结果单位与中文；
4. 解释完整性警告误报；
5. 机制链路本地化和订单聚合；
6. 历史页按账号重写；
7. 运行/治理页本地化。

### 阶段 D：生产化与长期治理

1. 真实供应商能力矩阵；
2. 价格快照自动过期；
3. 重启与部署自动回归；
4. 数据删除和账号隐私流程；
5. 外部验证与红队执行；
6. 具名目标用户测试。

---

## 21. 建议测试矩阵

### 21.1 AI 引导

- 无 AI 配置的确定性引导；
- 智谱思考关闭；
- 智谱思考开启但工作流自动关闭；
- 模型超时；
- 模型返回非 JSON；
- Schema 不合法；
- 数据库提交失败；
- 刷新、双标签页和重复点击；
- 恢复 UNKNOWN；
- 候选应用、拒绝、替换。

### 21.2 Factory

- 多份粘贴原文；
- 搜索线索；
- Reader 正文；
- 来源排除和撤销；
- 永久删除；
- 内容安全 REVIEW；
- 规则回退抽取；
- LLM 抽取；
- 重新抽取；
- 批量批准；
- 冻结。

### 21.3 情景与运行

- 合法干预；
- 机制缺失干预；
- 冻结场景另存新版本；
- RULE_ONLY；
- HYBRID_LLM；
- 费用不足但允许规则回退；
- 费用充足；
- 中途限流；
- 中途取消 LLM；
- 认知作用率为零。

### 21.4 结果与解释

- 区间跨零；
- 区间不跨零；
- 正负方向；
- 全部为零；
- 数据缺失；
- 规则模式；
- 部分回退；
- 全部回退；
- 解释 thinking on/off；
- Markdown；
- 证据引用一致、不一致和未知历史引用；
- 统计错误修复与确定性回退。

### 21.5 浏览器与可访问性

- Chrome、Safari；
- 桌面、移动端；
- 500px 窗口高度；
- 200% 缩放；
- 键盘；
- VoiceOver 基本流程；
- 中英文；
- 明暗主题；
- 减少动态效果。

### 21.6 部署

- 正常 GitHub 同步；
- CI 等待；
- CI 失败；
- 新版本健康失败回滚；
- Nginx 重启；
- Docker 重启；
- 整机重启；
- 宝塔任务日志；
- 宝塔流量；
- SSE；
- 公网 SHA。

---

## 22. 完成定义

不能以“页面不报错”作为本轮修复完成标准。至少满足：

1. 新用户可以不打开 F12 完成 AI 引导到实验结果；
2. 任意模型失败不会永久锁死工作流；
3. 用户选择混合 LLM 时，系统不会悄悄以规则模式冒充成功；
4. 结果解释中的数字、方向、区间和结论通过确定性校验；
5. 有效 LLM 认知信号是否作用于确定性策略可追踪；
6. 所有不可逆操作有明确确认和恢复替代路径；
7. 中文界面不暴露关键英文内部枚举；
8. 登录后的历史文案与真实账号持久化一致；
9. 当前部署 SHA、CI、服务健康和自动同步可核对；
10. 目标用户能在第一屏说明“这是给谁、做什么、不做什么”；
11. 三位具名目标用户完成测试，并把观察写回问题清单；
12. 所有修复经过单元测试、集成测试、生产构建和真实部署验证。

---

## 23. 最终判断

EventShock Lab 当前不是“只有演示页面的空壳”。确定性仿真、证据冻结、匹配随机种子、数据库持久化、历史实验、机制链路、可复现导出和服务器部署均已真实运行。

目前最主要的问题也不是“功能太少”，而是：

- AI 辅助链路在异常状态下不够可恢复；
- LLM 的结构化稳定性和实际机制作用没有达到可宣称水平；
- 结果解释的语言校验落后于 JSON 和引用校验；
- 专业信息很多，但用户不知道什么最重要；
- 部分旧文案和内部枚举与当前账号化、双语化、生产化状态不一致；
- 产品边界仍可能被误读为个人股票分析。

因此，下一轮不应继续无差别增加功能。应先把：

**可靠完成、真实作用、正确解释、清楚定位、可验证发布**

这五件事做成闭环，再扩展新的模型、事件和高级分析能力。
